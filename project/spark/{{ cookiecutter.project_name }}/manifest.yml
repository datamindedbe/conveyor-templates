features:
  - name: gradle
    enabled: {{cookiecutter.build_tool == "gradle"}}
    resources:
      - gradlew
      - gradlew.bat
      - settings.gradle
      - gradle.properties
      - build.gradle
      - gradle
