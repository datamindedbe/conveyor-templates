default:
  outputs:
    dev:
      type: duckdb
      path: /tmp/dbt.duckdb
      threads: 1
      extensions:
        - httpfs
        - parquet
      use_credential_provider: aws
    local:
      type: duckdb
      path: /workspace/dbt.duckdb
      threads: 1
  target: local